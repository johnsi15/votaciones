<?php
  /*if(isset($_POST['cleber'])){
  	  $cod = $_POST['cleber'];
  	 // echo " codigo ".$cod;
  	  require_once('funciones.php');
  	  $objeto = new funciones();
  	  $objeto->registrarVotos($cod);
  }

  if(isset($_POST['cesarC'])){
      $cod = $_POST['cesarC'];
  	 // echo " codigo ".$cod;
  	  require_once('funciones.php');
  	  $objeto = new funciones();
  	  $objeto->registrarVotos($cod);
  }

  if(isset($_POST['richar'])){
      $cod = $_POST['richar'];
  	 // echo " codigo ".$cod;
  	  require_once('funciones.php');
  	  $objeto = new funciones();
  	  $objeto->registrarVotos($cod);
  }
   
   if(isset($_POST['nelson'])){
      $cod = $_POST['nelson'];
  	 // echo " codigo ".$cod;
  	  require_once('funciones.php');
  	  $objeto = new funciones();
  	  $objeto->registrarVotos($cod);
   }

    if(isset($_POST['cesarL'])){
      $cod = $_POST['cesarL'];
  	 // echo " codigo ".$cod;
  	  require_once('funciones.php');
  	  $objeto = new funciones();
  	  $objeto->registrarVotos($cod);
   }

    if(isset($_POST['fabio'])){
      $cod = $_POST['fabio'];
  	 // echo " codigo ".$cod;
  	  require_once('funciones.php');
  	  $objeto = new funciones();
  	  $objeto->registrarVotos($cod);
   }

   if(isset($_POST['jesus'])){
        $cod = $_POST['jesus'];
        require_once('funciones.php');
        $objeto = new funciones();
        $objeto->registrarVotos($cod);
   }
   cerramos las votaciones
*/
?>